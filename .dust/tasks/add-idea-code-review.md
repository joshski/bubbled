# Add Idea: Code Review

Delete two failing tests from `dust-backlog.test.ts`.

The tests are `keeps task files scoped to documented breakdown tasks` and `references API_SKETCHES.md in every task`.

## Task Type

implement

## Blocked By

(none)


## Definition of Done

- The `keeps task files scoped to documented breakdown tasks` test is deleted from `dust-backlog.test.ts`
- The `references API_SKETCHES.md in every task` test is deleted from `dust-backlog.test.ts`
- The test suite no longer fails because of these backlog-only constraints
