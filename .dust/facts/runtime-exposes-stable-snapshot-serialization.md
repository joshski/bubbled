# Runtime exposes stable snapshot serialization

`serializeBubbleSnapshot()` projects a bubble snapshot into a deterministic serialized tree.
The output preserves tree order while sorting attribute and property keys for stable snapshots.
