# Bubble core builds runtime behaviors as deterministic slices

`bubble-core` keeps observable runtime behavior behind injected capabilities and deterministic in-memory defaults.
Its colocated tests exercise each runtime surface with explicit fakes, and the runtime foundations review found no immediate follow-up task for this package.
