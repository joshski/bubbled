# Runtime exposes deterministic storage

`createBubble()` provides a deterministic in-memory `storage` capability by default.
Each bubble instance gets its own storage state, so sessions remain isolated.
